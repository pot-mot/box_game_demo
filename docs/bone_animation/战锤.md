# 战锤

> 共用规范见 [`动作设计规范.md`](动作设计规范.md)。
> 状态：**已修正**（锤头对齐柄顶）。

## 武器配置

- id `war_hammer`，中文名「战锤」，`type: melee`（`melee_weapon.ts`）。
- 持握模式：**双手共持**（`twoHanded: true`），左手链 IK 贴合主手武器轴。
- 网格：`headSize = 0.35`、`color`、`gripColor`。

## 网格生成（`genWarHammer`）

```
hsz  = headSize = 0.35
gLen = hsz * 1.5 = 0.525
柄：cyl(r=0.04, h=gLen)，中心 y = -hsz*0.2 - gLen/2 = -0.3325
     → 柄范围 y ∈ [-0.595, -0.07]，柄顶 = -0.07
锤头：box(0.245 × 0.175 × 0.245)，中心 y = gLen*0.7 = 0.3675
     → 锤头范围 y ∈ [0.28, 0.455]，锤头底 = 0.28
```

## 问题与修正

原实现柄顶 `-0.07` 与锤头底 `0.28` 之间空 `0.35`，柄与锤头**断开**。

修正：锤头底对齐柄顶，`headY = handleTop + headH/2 = -hsz·0.2 + hsz·0.25 = hsz·0.05`；命中箱中心 y 与刀尖采样点同步为锤头中心/顶端。`gLen` 与 `gripY` 不变。

## 关联代码

- `src/entity/character/appearance/weapon_mesh.ts` `genWarHammer`（含 `finish(...)` 的命中箱参数）
- 固有握持：`src/entity/character/appearance/weapon_mesh.ts` `WEAPON_MESH_GRIPS.war_hammer`（烘焙进模型）

## 验收

骨骼动画模式手动装备战锤：柄与锤头连接为一体，命中箱环绕锤头。
