# 双斧

> 共用规范见 [`动作设计规范.md`](动作设计规范.md)。
> 状态：**已实现**（首把双持武器）。

## 武器配置

- id `dual_axe`，中文名「双斧」，`type: melee`（`melee_weapon.ts`）。
- 持握模式：**双持**（`twoHanded: false` + `offhandMesh` 存在）。
- 主手网格 `mesh: {id: 'dual_axe', bladeSize: 0.3, ...}`；副手 `offhandMesh: {..., mirror: true}`（斧刃开向 `-X`，两把刃口均朝外）。
- 网格 `genDualAxe` 生成**单刃斧**（柄 + 一片斧刃，刃接柄顶）；`mirror` 控制斧刃在 `±X`。

## 攻击动作

- 4 个段（轻 1 / 轻 2 / 重 1 / 重 2）的左右交替（相位错开半程 → 右手前半程挥出、左手在其恢复期挥出）已**烘焙进各段的关键帧**（`attack_clip_data.ts`），不再有 `mirrorLeftArmPhaseOffset` / `leftArm` / `phaseOffset` 参数。
- 命中事件按槽分组：主手一对（`params.weapon: 'main'`），副手一对（`'offhand'`）；近战执行器分别开关左右命中箱，**两只手都会结算伤害**。

## 关联代码

- `src/character/weapon/melee_weapon.ts`（`dual_axe` 预设：`mesh` / `offhandMesh`）
- `src/character/weapon/attack_clip_data.ts`（双斧 4 段的骨骼关键帧，含左右交替）
- `src/entity/character/appearance/weapon_mesh.ts` `genDualAxe`
- `src/entity/character/appearance/model.ts`（双武器装配）
- `src/entity/character/combat/melee_executor.ts`（按槽命中窗口）
- `src/entity/character/physics/world.ts`（事件 `params.weapon` → 命中窗口）

## 验收

- 展示模式双斧角色双手各持一把斧，4 段交替挥砍；
- 骨骼动画模式可见两个武器挂点、两把斧；
- 近战执行器在命中窗口内分别判定左右武器（单测 `melee_executor.test.ts` 覆盖）。
